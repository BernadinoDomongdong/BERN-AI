{
  "name": "bernardai-secure",
  "version": "1.1.0",
  "private": true,
  "description": "BernardAi chat UI. API key stays server-side in a Vercel serverless function, and only currently-free OpenRouter models are ever used.",
  "engines": {
    "node": ">=18"
  }
}
